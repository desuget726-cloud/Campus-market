import { useState } from 'react';

const settingsSections = [
    ['account', '👤', 'Account'],
    ['security', '🔐', 'Security'],
    ['notifications', '🔔', 'Notifications'],
    ['marketplace', '🛍️', 'Marketplace'],
    ['ai', '🤖', 'AI Advisor'],
    ['wallet', '💰', 'Wallet & Payment'],
    ['privacy', '🛡️', 'Privacy'],
    ['appearance', '🎨', 'Appearance'],
];

const Toggle = ({ label, checked, onChange }) => (
    <button type="button" onClick={onChange} className="flex w-full items-center justify-between rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-left text-sm font-semibold text-slate-700">
        <span>{label}</span>
        <span className={`relative h-6 w-11 rounded-full transition ${checked ? 'bg-emerald-500' : 'bg-slate-300'}`}><span className={`absolute top-1 h-4 w-4 rounded-full bg-white transition ${checked ? 'left-6' : 'left-1'}`} /></span>
    </button>
);

const Field = ({ label, children }) => <label className="block"><span className="block text-xs font-bold uppercase tracking-[0.16em] text-slate-500">{label}</span>{children}</label>;
const inputClass = 'mt-2 block w-full rounded-2xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-900 outline-none transition focus:border-emerald-500 focus:bg-white';

function SettingsCenter({ settingsTab, setSettingsTab, profileForm, handleProfileFieldChange, handleProfileSubmit, profileMessage, profileSaving, user, avatarUrl, avatarFile, setAvatarFile, handleAvatarUploadSubmit, avatarUploadMessage, avatarUploading, universityStructure, currentWalletBalance, transactionLedger, onNavigate }) {
    const [security, setSecurity] = useState({ twoFactor: true, currentPassword: '', newPassword: '', confirmPassword: '' });
    const [notificationPrefs, setNotificationPrefs] = useState({ messagesInApp: true, messagesEmail: true, ordersInApp: true, ordersEmail: true, paymentsInApp: true, paymentsEmail: false, browser: true });
    const [marketplace, setMarketplace] = useState({ radius: '5', pickup: 'Student Center', verifiedOnly: true, categories: ['Electronics', 'Books'] });
    const [aiPrefs, setAiPrefs] = useState({ frequency: 'Daily', departmentTriggers: true });
    const [paymentMethod, setPaymentMethod] = useState('Chapa');
    const [privacy, setPrivacy] = useState({ profileVisible: true, showPhone: false });
    const [appearance, setAppearance] = useState({ theme: 'System', language: 'English' });
    const [deleteOpen, setDeleteOpen] = useState(false);
    const [savedMessage, setSavedMessage] = useState('');
    const updatePref = (key) => setNotificationPrefs((previous) => ({ ...previous, [key]: !previous[key] }));
    const updateCategory = (category) => setMarketplace((previous) => ({ ...previous, categories: previous.categories.includes(category) ? previous.categories.filter((item) => item !== category) : [...previous.categories, category] }));
    const saveMessage = () => { setSavedMessage('Preferences saved for this session.'); window.setTimeout(() => setSavedMessage(''), 2500); };

    const renderPanel = () => {
        if (settingsTab === 'account') return <><PanelHeader eyebrow="Account" title="Your verified student profile" text="Keep your contact details current while protected academic identity fields remain read-only." /><div className="mt-6 grid gap-6 xl:grid-cols-[260px_1fr]"><div className="space-y-5 rounded-3xl border border-slate-200 bg-slate-50 p-5"><div className="flex flex-col items-center gap-4 text-center"><img src={avatarUrl || (user?.studentId ? `http://127.0.0.1:8000/static/uploads/avatars/${user.studentId}.jpg` : 'https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?auto=format&fit=crop&w=280&q=80')} alt="Profile avatar" onError={(event) => { event.currentTarget.onerror = null; event.currentTarget.src = 'https://images.unsplash.com/photo-1535713875002-d1d0cf377fde?auto=format&fit=crop&w=280&q=80'; }} className="h-32 w-32 rounded-full border border-slate-200 object-cover" /><div><p className="text-sm font-semibold text-slate-900">Profile Avatar</p><p className="text-xs text-slate-500">Upload a new photo from your computer.</p></div></div><label className="block text-sm font-semibold text-slate-700">Choose Image<input type="file" accept="image/*" onChange={(event) => setAvatarFile(event.target.files?.[0] || null)} className="mt-3 block w-full text-sm text-slate-700 file:mr-3 file:rounded-full file:border-0 file:bg-white file:px-3 file:py-2 file:text-sm file:font-semibold file:text-slate-700" /></label><button type="button" onClick={handleAvatarUploadSubmit} disabled={!avatarFile || avatarUploading} className="w-full rounded-full bg-sky-600 py-3 text-sm font-semibold text-white transition hover:bg-sky-700 disabled:cursor-not-allowed disabled:bg-slate-400">{avatarUploading ? 'Uploading...' : 'Upload Avatar'}</button>{avatarUploadMessage && <p className={`text-sm ${avatarUploadMessage.includes('failed') ? 'text-rose-600' : 'text-emerald-600'}`}>{avatarUploadMessage}</p>}</div><form onSubmit={handleProfileSubmit} className="grid gap-4 sm:grid-cols-2"><Field label="Full Name"><input value={profileForm.name} onChange={(event) => handleProfileFieldChange('name', event.target.value)} className={inputClass} /></Field><Field label="Student ID"><input value={profileForm.studentId} disabled className={`${inputClass} bg-slate-100 text-slate-500`} /></Field><Field label="Campus Email"><input type="email" value={profileForm.email} disabled className={`${inputClass} bg-slate-100 text-slate-500`} /></Field><Field label="Phone Number"><input type="tel" value={profileForm.phone} onChange={(event) => handleProfileFieldChange('phone', event.target.value)} className={inputClass} /></Field><Field label="Select College"><select value={profileForm.college} onChange={(event) => handleProfileFieldChange('college', event.target.value)} className={inputClass}><option value="">Select College</option>{Object.keys(universityStructure).map((college) => <option key={college} value={college}>{college}</option>)}</select></Field><Field label="Select Department"><select value={profileForm.department} onChange={(event) => handleProfileFieldChange('department', event.target.value)} disabled={!profileForm.college} className={`${inputClass} disabled:cursor-not-allowed disabled:opacity-60`}><option value="">Select Department</option>{profileForm.college && universityStructure[profileForm.college]?.map((department) => <option key={department} value={department}>{department}</option>)}</select></Field><Field label="New Password"><input type="password" value={profileForm.password} onChange={(event) => handleProfileFieldChange('password', event.target.value)} className={inputClass} /></Field><Field label="Confirm New Password"><input type="password" value={profileForm.confirmPassword} onChange={(event) => handleProfileFieldChange('confirmPassword', event.target.value)} className={inputClass} /></Field><div className="sm:col-span-2">{profileMessage && <p className={`mb-4 text-sm font-semibold ${profileMessage.includes('successfully') ? 'text-emerald-600' : 'text-rose-600'}`}>{profileMessage}</p>}<button type="submit" disabled={profileSaving} className="rounded-full bg-emerald-500 px-5 py-3 text-sm font-bold text-white hover:bg-emerald-600 disabled:cursor-not-allowed disabled:bg-slate-300">{profileSaving ? 'Saving...' : 'Save Changes'}</button></div></form></div></>;
        if (settingsTab === 'security') return <><PanelHeader eyebrow="Security" title="Protect your account" text="Review password access, two-factor protection, and recent sessions." /><div className="mt-6 grid gap-6 lg:grid-cols-2"><div className="space-y-4"><Field label="Current Password"><input type="password" value={security.currentPassword} onChange={(event) => setSecurity({ ...security, currentPassword: event.target.value })} className={inputClass} /></Field><Field label="New Password"><input type="password" value={security.newPassword} onChange={(event) => setSecurity({ ...security, newPassword: event.target.value })} className={inputClass} /></Field><Field label="Confirm New Password"><input type="password" value={security.confirmPassword} onChange={(event) => setSecurity({ ...security, confirmPassword: event.target.value })} className={inputClass} /></Field><button type="button" onClick={saveMessage} className="rounded-full bg-slate-900 px-5 py-3 text-sm font-bold text-white">Update Password</button></div><div className="space-y-4"><Toggle label="Two-Factor Authentication" checked={security.twoFactor} onChange={() => setSecurity({ ...security, twoFactor: !security.twoFactor })} /><div className="rounded-2xl border border-emerald-200 bg-emerald-50 p-4"><p className="text-xs font-bold uppercase tracking-wider text-emerald-700">Active session</p><p className="mt-2 font-bold text-slate-900">This device · 127.0.0.1</p><p className="mt-1 text-sm text-slate-600">Last active just now · Protected connection</p></div></div></div></>;
        if (settingsTab === 'notifications') return <><PanelHeader eyebrow="Notifications" title="Choose what reaches you" text="Control in-app, email, and browser alerts for important marketplace activity." /><div className="mt-6 space-y-5">{[['New Messages', 'messagesInApp', 'messagesEmail'], ['Order Updates', 'ordersInApp', 'ordersEmail'], ['Payment Success', 'paymentsInApp', 'paymentsEmail']].map(([label, inApp, email]) => <div key={label} className="grid gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-4 sm:grid-cols-[1fr_auto_auto] sm:items-center"><span className="font-bold text-slate-800">{label}</span><label className="flex items-center gap-2 text-sm text-slate-600"><input type="checkbox" checked={notificationPrefs[inApp]} onChange={() => updatePref(inApp)} /> In-app</label><label className="flex items-center gap-2 text-sm text-slate-600"><input type="checkbox" checked={notificationPrefs[email]} onChange={() => updatePref(email)} /> Email</label></div>)}<Toggle label="Browser notifications" checked={notificationPrefs.browser} onChange={() => updatePref('browser')} /></div></>;
        if (settingsTab === 'marketplace') return <><PanelHeader eyebrow="Marketplace" title="Personalize discovery" text="Tune search, pickup, and seller trust preferences for campus shopping." /><div className="mt-6 space-y-5"><Field label={`Search Radius: ${marketplace.radius} km`}><input type="range" min="1" max="25" value={marketplace.radius} onChange={(event) => setMarketplace({ ...marketplace, radius: event.target.value })} className="mt-4 w-full accent-emerald-500" /></Field><Field label="Preferred Campus Pickup"><select value={marketplace.pickup} onChange={(event) => setMarketplace({ ...marketplace, pickup: event.target.value })} className={inputClass}><option>Student Center</option><option>Main Library</option><option>Science Block</option><option>ICT Service Desk</option></select></Field><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-slate-500">Favorite Categories</p><div className="mt-3 flex flex-wrap gap-2">{['Electronics', 'Books', 'Clothing'].map((category) => <button key={category} type="button" onClick={() => updateCategory(category)} className={`rounded-full px-4 py-2 text-sm font-bold ${marketplace.categories.includes(category) ? 'bg-emerald-500 text-white' : 'bg-slate-100 text-slate-600'}`}>{category}</button>)}</div></div><Toggle label="Show only verified sellers" checked={marketplace.verifiedOnly} onChange={() => setMarketplace({ ...marketplace, verifiedOnly: !marketplace.verifiedOnly })} /></div></>;
        if (settingsTab === 'ai') return <><PanelHeader eyebrow="AI Advisor" title="Tune your recommendations" text="Control how often the marketplace advisor refreshes suggestions for your academic context." /><div className="mt-6 space-y-5"><Field label="Recommendation Frequency"><select value={aiPrefs.frequency} onChange={(event) => setAiPrefs({ ...aiPrefs, frequency: event.target.value })} className={inputClass}><option>Real-time</option><option>Daily</option><option>Weekly</option></select></Field><Toggle label="Use department-specific triggers" checked={aiPrefs.departmentTriggers} onChange={() => setAiPrefs({ ...aiPrefs, departmentTriggers: !aiPrefs.departmentTriggers })} /><button type="button" onClick={() => setAiPrefs({ frequency: 'Daily', departmentTriggers: true })} className="rounded-full border border-rose-200 bg-rose-50 px-5 py-3 text-sm font-bold text-rose-700">Reset AI preferences</button></div></>;
        if (settingsTab === 'wallet') return <><PanelHeader eyebrow="Wallet & Payment" title="Manage your campus balance" text="Review your balance, preferred payment provider, and transaction history." /><div className="mt-6 rounded-2xl bg-slate-950 p-5 text-white"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Available balance</p><p className="mt-2 text-3xl font-black">{Number(currentWalletBalance).toLocaleString('en-ET')} ETB</p></div><div className="mt-5 grid gap-4 sm:grid-cols-2"><Field label="Payment Method"><select value={paymentMethod} onChange={(event) => setPaymentMethod(event.target.value)} className={inputClass}><option>Chapa</option><option>SantimPay</option></select></Field><div><p className="text-xs font-bold uppercase tracking-[0.16em] text-slate-500">History</p><button type="button" onClick={() => onNavigate('buyer')} className="mt-2 text-sm font-bold text-sky-700 hover:text-sky-900">View deposit history →</button><button type="button" onClick={saveMessage} className="mt-2 block text-sm font-bold text-sky-700 hover:text-sky-900">Refund confirmations →</button></div></div><div className="mt-5 space-y-2">{transactionLedger.slice(0, 3).map((transaction) => <div key={transaction.id} className="flex justify-between rounded-xl bg-slate-50 p-3 text-sm"><span className="text-slate-600">{transaction.label || transaction.type}</span><span className="font-bold text-slate-900">{Number(transaction.amount || 0).toLocaleString('en-ET')} ETB</span></div>)}</div></>;
        if (settingsTab === 'privacy') return <><PanelHeader eyebrow="Privacy" title="Control your campus visibility" text="Decide what other students can see when they interact with you." /><div className="mt-6 space-y-4"><Toggle label="Show profile on campus directory" checked={privacy.profileVisible} onChange={() => setPrivacy({ ...privacy, profileVisible: !privacy.profileVisible })} /><Toggle label="Show phone number to buyers" checked={privacy.showPhone} onChange={() => setPrivacy({ ...privacy, showPhone: !privacy.showPhone })} /><button type="button" onClick={() => setDeleteOpen(true)} className="mt-4 rounded-full border border-rose-200 bg-rose-50 px-5 py-3 text-sm font-bold text-rose-700">Delete Account</button></div>{deleteOpen && <div className="fixed inset-0 z-[100] flex items-center justify-center bg-slate-950/60 p-4"><div className="w-full max-w-md rounded-3xl bg-white p-6 shadow-2xl"><h3 className="text-xl font-black text-slate-950">Delete your account?</h3><p className="mt-3 text-sm leading-6 text-slate-600">This destructive action requires confirmation. Your marketplace records may be retained for audit purposes.</p><div className="mt-6 flex justify-end gap-3"><button type="button" onClick={() => setDeleteOpen(false)} className="rounded-full border border-slate-200 px-4 py-2 text-sm font-bold text-slate-700">Cancel</button><button type="button" onClick={() => { setDeleteOpen(false); setSavedMessage('Account deletion request requires support verification.'); }} className="rounded-full bg-rose-600 px-4 py-2 text-sm font-bold text-white">Confirm Request</button></div></div></div>}</>;
        return <><PanelHeader eyebrow="Appearance" title="Make the dashboard yours" text="Choose a visual theme and language for your marketplace workspace." /><div className="mt-6 grid gap-5 sm:grid-cols-2"><Field label="Theme"><select value={appearance.theme} onChange={(event) => setAppearance({ ...appearance, theme: event.target.value })} className={inputClass}><option>Light</option><option>Dark</option><option>System</option></select></Field><Field label="Language"><select value={appearance.language} onChange={(event) => setAppearance({ ...appearance, language: event.target.value })} className={inputClass}><option>English</option><option>Amharic</option></select></Field></div></>;
    };

    return <div className="min-h-screen bg-slate-50 px-4 pb-10 pt-16 text-slate-900 lg:px-8 lg:pt-10"><div className="mx-auto max-w-7xl"><div className="mb-6"><p className="text-xs font-bold uppercase tracking-[0.25em] text-emerald-600">Student Control Center</p><h2 className="mt-2 text-3xl font-black text-slate-950">Account & Marketplace Settings</h2></div><div className="grid gap-6 lg:grid-cols-[240px_1fr]"><aside className="h-fit rounded-[28px] border border-slate-200 bg-white p-3 shadow-sm"><nav className="space-y-1">{settingsSections.map(([id, icon, label]) => <button key={id} type="button" onClick={() => setSettingsTab(id)} className={`flex w-full items-center gap-3 rounded-2xl px-4 py-3 text-left text-sm font-bold transition ${settingsTab === id ? 'bg-slate-900 text-white shadow-sm' : 'text-slate-600 hover:bg-slate-50 hover:text-slate-950'}`}><span className="text-lg">{icon}</span>{label}</button>)}</nav></aside><section className="rounded-[28px] border border-slate-200 bg-white p-6 shadow-sm sm:p-8">{renderPanel()}{savedMessage && <p className="mt-6 rounded-2xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-bold text-emerald-700">{savedMessage}</p>}</section></div></div></div>;
}

function PanelHeader({ eyebrow, title, text }) {
    return <div className="border-b border-slate-200 pb-5"><p className="text-xs font-bold uppercase tracking-[0.22em] text-sky-600">{eyebrow}</p><h3 className="mt-2 text-2xl font-black text-slate-950">{title}</h3><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">{text}</p></div>;
}

export default SettingsCenter;
